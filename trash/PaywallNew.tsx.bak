import React from 'react';
import RevenueCatPaywall from './RevenueCatPaywall';

interface PaywallProps {
  onClose?: () => void;
}

// Legacy Paywall component - now redirects to RevenueCat implementation
export const Paywall: React.FC<PaywallProps> = ({ onClose }) => {
  return <RevenueCatPaywall onClose={onClose} />;
};

export default Paywall;
