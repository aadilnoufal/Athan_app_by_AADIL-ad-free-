/**
 * Expo Config Plugin
 * This is a placeholder plugin file created to resolve a plugin resolution error.
 * If you don't need custom native modifications, this file can remain empty.
 */

module.exports = function withCustomPlugin(config) {
  // Return the config unchanged
  return config;
};
